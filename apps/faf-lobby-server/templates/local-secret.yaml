{{- if not (index .Values "infisical-secret" "enabled") }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Chart.Name }}
type: Opaque
stringData:
  DB_PASSWORD: "banana"
  MQ_PASSWORD: "banana"
{{- end}}
